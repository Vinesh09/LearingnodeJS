const express = require("express");
const view = require("../Controller(views)/auth");
const router = express.Router();

// router.post("/logIn", view.login).post("/signUp", view.createUser);

// module.exports = router;
